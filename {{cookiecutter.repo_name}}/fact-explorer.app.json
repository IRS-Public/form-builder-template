{
  "$comment": [
    "This app, as Fact Explorer sees it.",
    "",
    "The descriptor lives here rather than inside Fact Explorer so the values sit beside the",
    "Main.scala they mirror, and so this repo stays self-contained. Fact Explorer's",
    "scripts/build-registry.mjs globs ../*/fact-explorer.app.json and merges every one it finds — so",
    "this file IS the registration. Put this repo beside fact-explorer and it appears in the app",
    "picker; there is no list to edit.",
    "",
    "Keep in step with src/main/scala/.../Main.scala and this repo's Makefile PORT."
  ],

  "id": "{{ cookiecutter.app_id }}",
  "label": "{{ cookiecutter.project_name }}",
  "appId": "{{ cookiecutter.app_id }}",
  "basePath": "/app/{{ cookiecutter.url_segment }}",

  "storagePrefix": "{{ cookiecutter.storage_prefix }}",
  "devPort": {{ cookiecutter.dev_port }},

  "resourceRoot": "src/main/resources",

  "engine": {
    "bundle": "resources/vendor/fact-graph/factgraph-{{ cookiecutter.factgraph_version|replace('-SNAPSHOT', '') }}.js",
    "dictionary": "resources/fact-dictionary.xml"
  },

  "$comment_capabilities": [
    "What this app was generated with. Fact Explorer prunes its menu from these: an app without",
    "allScreens gets no Browse All or Path Mode entry, because those pages do not exist and the link",
    "would 404."
  ],
  "capabilities": {
    "allScreens": true,
    "scenarioMode": true,
    "authorMode": true
  },

  "$comment_scenarios": [
    "null when this app has no scenario corpus — Fact Explorer then shows a scenario picker with no",
    "filter dropdowns rather than a broken one. `vocabulary` names a filter set in",
    "fact-explorer/src/model/scenarios/; 'none' is the floor and is always available."
  ],
  "scenarios": {
    "dir": "scenarios",
    "vocabulary": "none"
  },

  "$comment_customFlowTags": [
    "Mirror FormativeApp.nodeTypes here. A flow tag the scaffold has never heard of reaches the graph",
    "as a real node only if it is declared — undeclared tags are rejected, which is what makes a typo",
    "findable rather than silently invisible."
  ],
  "customFlowTags": []
}
