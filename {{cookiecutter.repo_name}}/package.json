{
  "name": "{{ cookiecutter.repo_name }}",
  "private": true,
  "description": "Sibling-package dependencies for {{ cookiecutter.project_name }}. The app itself has no bundler; this exists so `make copy-shared-ui` has a node_modules to mirror from.",
  "devDependencies": {
    "taxpert": "file:../taxpert"
  }
}
